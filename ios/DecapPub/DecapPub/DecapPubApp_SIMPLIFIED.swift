import SwiftUI

// NOTE: This file should be DELETED from your Xcode project
// It's a duplicate and causes "Invalid redeclaration of 'DecapPubApp'" error
// Keep only the main DecapPubApp.swift file

/*
struct DecapPubApp_Backup: App {
    
    var body: some Scene {
        WindowGroup {
            if #available(iOS 26.0, *) {
                ContentView()
            } else {
                Text("This app requires iOS 26.0 or later")
                    .font(.title2)
                    .padding()
            }
        }
    }
}
*/

// Temporary: Copy this over DecapPubApp.swift if you need to build immediately
// Then add the new files to your project and restore the full version
